name: booktickets
description: "Flutter BookTickets project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  fluentui_icons: ^1.0.0
  get: ^4.6.6
  gap: ^3.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.1
  barcode_widget: ^2.0.2

flutter:

  uses-material-design: true
  assets:
    - assets/